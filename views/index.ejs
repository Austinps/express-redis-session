<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Homepage</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        max-width: 500px;
        margin: 0 auto;
        padding: 20px;
      }

      h1 {
        text-align: center;
      }

      a {
        display: block;
        margin: 10px 0;
      }

      .loggedIn {
        border: 1px solid #ccc;
        padding: 20px;
        margin-top: 30px;
      }

      .loggedIn p {
        margin: 0;
      }

      .loggedIn a {
        display: inline-block;
        margin: 10px;
      }
    </style>
  </head>
  <body>
    <h1>Homepage</h1>
    <% if (loggedIn) { %>
    <div class="loggedIn">
      <% if (user) { %>
      <p>You are logged in as <%= user.username %></p>
      <a href="/protected">Protected view</a>
      <a href="/logout">Logout</a>
      <% } else { %>
      <p>You are logged out.</p>
      <a href="/login">Login</a>
      <% } %>
    </div>
    <% } else { %>
    <p>Welcome to the site.</p>
    <a href="/login">Login</a>
    <a href="/signup">SignUp</a>
    <% } %>
  </body>
</html>
